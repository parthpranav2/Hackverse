<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Background Color -->
    <solid android:color="#64C466" />

    <!-- Rounded Corners -->
    <corners android:radius="8.333dp" />

    <!-- Optional Padding (if needed) -->
    <padding
        android:left="0dp"
        android:top="0dp"
        android:right="0dp"
        android:bottom="0dp" />

</shape>
